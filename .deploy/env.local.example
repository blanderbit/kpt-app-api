# Environment
NODE_ENV=development

# Database Configuration
DATABASE_HOST=mysql
DATABASE_PORT=3306
DATABASE_NAME=kpt_dev
DATABASE_USER=kpt_user
DATABASE_PASSWORD=kpt_password
MYSQL_ROOT_PASSWORD=root_password

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-development
JWT_EXPIRES_IN=6d
JWT_REFRESH_SECRET=your-super-secret-refresh-key-development
JWT_REFRESH_EXPIRES_IN=7d

# Email Configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_FROM=noreply@kpt-app.com

# Frontend URL
FRONTEND_URL=http://localhost:3000
CORS_ORIGINS=http://localhost:8085,http://localhost:3000

# MinIO Configuration
MINIO_HOST=minio
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# MySQL Environment Variables
MYSQL_DATABASE=kpt_dev
MYSQL_USER=kpt_user
MYSQL_PASSWORD=kpt_password

# phpMyAdmin Environment Variables
PMA_HOST=mysql
PMA_PORT=3306
PMA_USER=kpt_user
PMA_PASSWORD=kpt_password

# MinIO Environment Variables
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=redispassword_dev
REDIS_DB=0
REDIS_PREFIX=kpt

# Google Drive Configuration
GOOGLE_DRIVE_KEY_FILE=./config/third-party/google-drive.json
GOOGLE_DRIVE_ROOT_FOLDER_ID=your-google-drive-root-folder-id

# Data Files Configuration
ACTIVITY_TYPES_FILE_ID=your-activity-types-file-id
MOOD_TYPES_FILE_ID=your-mood-types-file-id
SOCIAL_NETWORKS_FILE_ID=your-social-networks-file-id
ONBOARDING_QUESTIONS_FILE_ID=your-onboarding-questions-file-id
LANGUAGES_FOLDER_ID=your-languages-folder-id
BACKUP_FOLDER_ID=your-backup-folder-id
ARCHIVE_FOLDER_ID=your-archive-folder-id
SETTINGS_FILE_ID=your-settings-file-id

# Ports
API_PORT=3000
SWAGGER_PORT=3000
MYSQL_PORT=3307
PHPMYADMIN_PORT=8081
REDIS_PORT=6379
PORT=3000

# Domains
API_DOMAIN=api.kpt.local
PHPMYADMIN_DOMAIN=phpmyadmin.kpt.local
MINIO_DOMAIN=minio.kpt.local
MINIO_CONSOLE_DOMAIN=minio-console.kpt.local
TRAEFIK_DOMAIN=traefik.kpt.local
ADMIN_DOMAIN=admin.kpt.local

# Admin Frontend
VUE_APP_API_BASE_URL=http://localhost:3000

# RevenueCat
REVENUECAT_API_KEY=
REVENUECAT_API_URL=https://api.revenuecat.com/v1
REVENUECAT_WEBHOOK_AUTH=

# OpenAI
OPENAI_API_KEY=your-openai-api-key

# Activity create: max wait (ms) for ChatGPT to classify type before returning response (fallback in code: 3000)
CREATE_ACTIVITY_CLASSIFY_TIMEOUT_MS=3000

# Other
USERS_PAGE_SIZE=10
USERS_PAGE_DELAY=1
MINIO_PUBLIC_ENDPOINT=http://localhost:9002

PROGRAMS_FILE_ID=

# External signup: base URL to open mobile app after payment (user gets token and opens app already logged in).
REGISTRATION_LINK_BASE=https://app.plesury.com/onboarding

# Paddle (only if backend creates checkout via Paddle API; if using RevenueCat Web Purchase Links, leave empty)
# Sandbox: sandbox-vendors.paddle.com → API key pdl_sdbx_..., Price IDs из Sandbox Catalog.
# Production: vendors.paddle.com → API key pdl_live_..., Price IDs из Production Catalog.
PADDLE_API_KEY=
# sandbox | production
PADDLE_ENVIRONMENT=sandbox
# From Paddle: Catalog → Products → [product] → Prices → Price ID (e.g. pri_01...); для sandbox — свои ID из Sandbox.
PADDLE_PRICE_ID_MONTHLY=
PADDLE_PRICE_ID_YEARLY=
# Only if external checkout offers "Monthly (with trial)" plan
PADDLE_PRICE_ID_MONTHLY_TRIAL=
